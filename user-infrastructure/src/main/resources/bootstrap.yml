spring:
  application:
    name: user-application

---
spring:
  config:
    activate:
      on-profile: "!kubernetes"
  cloud:
    config:
      enabled: true
      fail-fast: true
      name: user-application, mongo, rabbit, swagger
      uri: http://localhost:1000/configuration